<script setup>
import { computed } from 'vue'
const props=defineProps({rows:{type:Array,default:()=>[]},leftLabel:String,rightLabel:String})
const bounded=value=>Math.max(0,Math.min(100,Number(value||0)))
</script>
<template><div class="metric-comparison"><header><b>{{leftLabel}}</b><span>MATCHUP PROFILE</span><b>{{rightLabel}}</b></header><article v-for="row in rows" :key="row.label"><strong>{{row.leftDisplay??row.left}}</strong><div><span>{{row.label}}</span><i><em :style="{width:`${bounded(row.left)}%`}"></em><b :style="{width:`${bounded(row.right)}%`}"></b></i></div><strong>{{row.rightDisplay??row.right}}</strong></article></div></template>
<style scoped>.metric-comparison{display:grid}.metric-comparison>header,.metric-comparison article{display:grid;grid-template-columns:74px 1fr 74px;gap:15px;align-items:center}.metric-comparison>header{padding:0 0 13px;font:700 8px 'DM Mono';color:var(--muted)}.metric-comparison>header b:last-child{text-align:right}.metric-comparison>header span{text-align:center}.metric-comparison article{min-height:60px;border-top:1px solid var(--line)}.metric-comparison article>strong{font:800 15px 'DM Mono'}.metric-comparison article>strong:last-child{text-align:right}.metric-comparison article>div{display:grid;gap:7px}.metric-comparison article span{text-align:center;font:700 7px 'DM Mono';color:var(--muted)}.metric-comparison article i{height:6px;display:flex;justify-content:center;gap:2px}.metric-comparison article i em,.metric-comparison article i b{height:100%;max-width:50%;transition:width .55s}.metric-comparison article i em{margin-left:auto;background:var(--blue);transform-origin:right}.metric-comparison article i b{margin-right:auto;background:var(--sport,var(--accent))}@media(max-width:480px){.metric-comparison>header,.metric-comparison article{grid-template-columns:48px 1fr 48px;gap:8px}}</style>
